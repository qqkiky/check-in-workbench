# 期待 · 本轮迭代概览（2026-08-01）

## 已交付功能
1. **软件更名**：「Do · 打卡工作台」→ **期待**（标题、PWA 名称、关于页、配置同步更新）。
2. **DO 页四时间维度**：本日 / 本周 / 当月 / 今年 分段切换，分别展示该时间段内应完成的任务列表；头部带完成进度条（如 `3/8 · 38%`）。由 store 新选择器 `getTasksByRange` 驱动（重复任务按星期、一次性任务按创建日落在范围内）。
3. **小事**：DO 页新增「小事」标签，输入框回车即可添加轻量临时打卡（无计划时间/目标），行内勾选完成、可归档。
4. **记事本**：「更多 → 记事本」进入，支持随手新增、查看、删除（本地持久化，已纳入备份/导入范围）。
5. **桌面小组件**：新增独立页 `/widget.html`，可「添加到主屏幕」作为桌面 widget，实时显示今日待办列表 + 进度条 + 小事勾选；与 App 共用同一本地数据，离线可用（service worker 已缓存）。

## 构建与部署
- `tsc --noEmit` 与 `vite build` 均零错误；`dist/` 含 `index.html` 与 `widget.html` 两个入口。
- 已重新部署 CloudStudio（同 sandbox，URL 不变）。
- 线上地址：https://74a2a90b75584bceb0dd46cd1d7430ca.bj8.agentos-app.net
- 桌面小组件地址：https://74a2a90b75584bceb0dd46cd1d7430ca.bj8.agentos-app.net/widget.html

## 待真机验证
- 四维度切换是否正常、进度计算正确
- 小事增/勾/归档
- 记事本增/删
- 桌面 widget 添加到主屏后，在 App 内勾选能否在 widget 实时反映（依赖 30s 轮询 + 窗口聚焦刷新）
