# 打卡工作台 v21 — DO 周打卡：按周切换 + 限定补卡

## 一、按周切换查看任意过往周次
- 周视图顶部新增周次导航：`‹ [本周 / N 周前] 8/3 - 8/9 ›`，点击左右箭头切换。
- 可向前查看**任意过往周次**的打卡记录与详情；下一周按钮在当前周禁用（不可查看未来周）。
- 点击某天仍跳转到该天的日视图（含补卡能力）；顶部切到「日/小事」会自动回到本周。

## 二、限定周间隔的补卡（可追溯）
- 在当前周可对**近 4 周**内遗漏的打卡进行补卡（`MAX_MAKEUP_WEEKS = 4`，集中可改）。
- 超过 4 周的遗漏打卡按钮**置灰禁用**，显示「超期不可补」，点击提示「仅可补近 4 周的遗漏打卡」。
- **数据归属准确**：补卡记录的 `date` 仍为原任务应完成日（归属原周次），查看对应周时正确归位；当前周不显示该记录。
- **补卡时间可追溯**：新增 `makeupAt` 时间戳，已补卡 badge 悬停显示「补卡于 MM/dd HH:mm」。

## 三、UI 反馈
- 周视图查看过往周时显示橙色提示条：说明可补卡范围与追溯规则。
- 待补卡任务显示「待补卡」标签；已补卡显示「已补卡」标签（带时间追溯）。
- 周进度文案随周次变为「本周 / 该周 X/Y 已完成」。

## 验证
- `tsc --noEmit` 通过；`vite build` 成功（877 模块）。
- 涉及文件：`utils/date.ts`、`types/index.ts`、`screens/HomeScreen.tsx`、`components/tasks/TaskCard.tsx`、`version.ts`。
- 部署链接（强刷清缓存）：https://74a2a90b75584bceb0dd46cd1d7430ca.bj8.agentos-app.net
