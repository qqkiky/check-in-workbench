# 移动端 APP 开发 · 配合说明（Runbook）

> 目的：明确接下来每个阶段「你需要做什么」「我会做什么」「哪些决定需要你拍板」，
> 让你能照单配合，不卡流程、不模糊。
> 工程就在你本机 `C:\Users\zqkik\WorkBuddy\打卡工作台`，我已用 `cap sync` 把当前 Web 产物注入 `android/` 原生壳，复用度已验证。

---

## 〇、先记住两条预览通道（理解后协作不混乱）

| 通道 | 谁负责运行 | 你能看到什么 | 适合验证 |
|---|---|---|---|
| **A. PWA 链接**（CloudStudio） | 【我】自动构建+部署 | 手机浏览器打开 `https://74a2a90b75584bceb0dd46cd1d7430ca.bj8.agentos-app.net`，「添加到主屏幕」即类 APP | UI、交互、业务逻辑、记账/打卡/统计 |
| **B. Android 原生**（Android Studio） | 【你】本机打开 `android/` 点 Run | 真机/模拟器里的原生壳 APP | 系统通知、触感、安全区、返回手势、小组件、性能 |

**分工铁律**：Web 代码我写、我构建；原生壳的「运行/上架」在你机器完成（沙箱无 Android SDK）。
每轮我改完 Web 代码会跑 `cap sync` 更新 `android/` 资源，你**直接 Android Studio 点 Run** 即可，无需 git pull。

---

## 阶段 0 · 本机环境就绪【你做】

按顺序执行，任一步卡住把报错原文发我：

1. 安装 **Android Studio**（官网下载，安装向导勾选 Android SDK）。
2. 打开 SDK Manager，安装：
   - Android SDK Platform（选你手机对应的 API，如 API 34）
   - Android SDK Build-Tools
   - Android SDK Platform-Tools
   - （可选）NDK，部分原生插件才需要
3. 配置环境变量（Windows）：
   - `ANDROID_HOME` = 你的 SDK 路径（如 `C:\Users\zqkik\AppData\Local\Android\Sdk`）
   - 把 `%ANDROID_HOME%\platform-tools` 加入 `Path`
4. 真机调试：手机「设置→关于手机→连续点版本号」开开发者选项 → 开「USB 调试」→ USB 连电脑并点允许。
   （或）模拟器：AVD Manager 建一台 Pixel 设备并启动。
5. Android Studio → Open → 选 `C:\Users\zqkik\WorkBuddy\打卡工作台\android`。
6. 点 Run（绿三角 / `Shift+F10`）→ 选设备。首次会 Gradle 同步并下载依赖（需联网，几分钟）。
7. 手机出现工作台界面 = 环境就绪。

> 若报 `SDK location not found`：在 `android/local.properties` 写 `sdk.dir=C\:\\Users\\zqkik\\AppData\\Local\\Android\\Sdk`。
> 若 Gradle 下载慢：告诉我，我给你国内镜像配置。

---

## 阶段 1 · 首次真机验证【你做 + 我交付】

- 【我】已执行 `cap sync android`，`android/app/src/main/assets/public/index.html` 引用的是当前 v23 产物。
- 【你】阶段 0 第 6 步 Run 成功后，确认能看到：首页打卡列表、记账页、时间线、回顾。
- 【你】回我一句话：「原生壳跑通，界面正常 / 异常现象是 ___」。异常请把 Android Studio 的 Logcat 报错或截图发我。

---

## 阶段 2 · MVP 实施：分工与你的配合

以下子任务我会逐个实现并部署。每个任务你需要做的已单列。

### 2.1 导航重构（底部 Tab + 中间 FAB + 安全区）【我做主，你确认】
- 【你】确认底部 5 Tab 顺序：**Do(打卡) · 记账 · [＋ FAB 记一笔] · 时间线 · 更多**。如想调整顺序/增删，现在说。
- 【你】确认主色：沿用现有蓝 `#3B82F6`、浅灰底 `#F2F2F7`（来自 manifest）。要换品牌色现在提。

### 2.2 打卡即时化 + 系统通知【我做，你测】
- 【我】把「进弹窗选时间」改为首页大按钮即时打卡（用当前时间）；补卡/改时间收到次级入口（复用已做的选日期逻辑）；接 `Capacitor.LocalNotifications` 实现到点提醒。
- 【你】在阶段 0 的 Android Studio Run 后，**手机弹窗授权通知**；告诉我通知是否准时到达。

### 2.3 记账 FAB【我做，你测】
- 【我】底部 FAB 展开「支出 / 收入 / 转账」，复用现有 `TransactionModal` / `TransferModal`。
- 【你】真机点 FAB 验证三项都能开、数字键盘正常、金额输入不串。

### 2.4 周/月回顾图表【我做，你测】
- 【我】复用 `WeeklyReview` / `MonthlyReview` + recharts，竖屏重排；年回顾/趋势图延后。
- 【你】真机看图表是否掉帧、文字是否溢出。

### 2.5 原生触感【我做，你测】
- 【我】把 `hapticsEnabled` 接 `Capacitor.Haptics.vibrate()`（打卡成功、FAB 展开等节点）。
- 【你】手机「设置→更多」里开/关触感开关，确认有/无震动反馈。

---

## 阶段 3 · 每轮开发后的验证循环【关键，你做】

每完成一个子任务，循环如下（请严格走，避免反复）：

1. 【我】写代码 → `build`（含安全删除绕过）→ `cap sync android` → （可选）部署 PWA 链接。
2. 【你·通道 A】手机浏览器打开 PWA 链接，硬刷新（清除旧 SW）测 UI/逻辑。
3. 【你·通道 B】Android Studio 点 Run 测原生行为（通知/触感/安全区）。
4. 【你】按下方模板给我反馈。

**反馈模板（直接复制填）**：
```
【现象】xxx 不对 / 报错
【通道】PWA 链接 / Android 原生
【设备】机型 + 系统版本（如 Pixel 7 / Android 14）
【期望】应该 ___ 
【实际】却是 ___
【截图/Logcat】附上
```

---

## 阶段 4 · 原生能力接入：权限与凭证【你提供】

| 能力 | 你需要做的 |
|---|---|
| 系统通知 | 真机首次弹窗点「允许」；如需服务端推送（多端提醒），你提供 FCM / Supabase 配置或让我建 |
| 生物识别锁 | 我做完后你用指纹/ Face 解锁测试 |
| 主屏小组件 | 我出 Widget；你在桌面长按添加并反馈显示 |
| **iOS 上架** | 你提供 **Apple Developer 账号（年费）** + 一台 Mac（archive 需 macOS）；我出 iOS 工程 |

---

## 阶段 5 · 上架准备【你提供素材】

Android 上架 Google Play 前，你需给我/自备：

1. **签名密钥**：我指导生成 `keystore`（或你给我的 `upload-keystore.jks` + 密码/别名）。
2. **图标**：`512×512` PNG（已有 `icon-512.png`，可复用；如需新品牌图你给）。
3. **截图**：手机端各页面截图 3–5 张（你_RUN 后截，或我生成占位）。
4. **隐私政策**：记账/打卡属个人敏感数据，上架需隐私政策 URL（你提供或我起草模板你填）。
5. **应用名称/简介**：默认「期待 · 打卡工作台」，可改。

---

## 阶段 6 · 云同步（可选）【你拍板】

- 【你】决定：**现在做 / 暂不做**。
- 若做：选 **Supabase**（推荐，Postgres+Auth+Realtime 一体）或 **Firebase**。
- 【你】确认敏感数据（记账/打卡）是否允许上云；默认我建议「不自动开，设置里显式授权」。

---

## 你需要现在拍板的关键决策（回我选项即可，避免我阻塞）

1. MVP 范围：接受建议书的 P0 全集（打卡+记账+回顾+原生导航）？还是先砍到更小？
2. 底部 Tab 顺序：用建议的 `Do · 记账 · ＋ · 时间线 · 更多`？还是要调？
3. 品牌色：沿用蓝 `#3B82F6`？还是换？
4. 云同步：现在做 / 暂不做？
5. iOS：要不要一并做（需你给 Apple 账号+Mac）？

---

## 我不做 / 需你自己做的

- 原生工程在你的 Android Studio 里 **Run / 构建 / 上架**：沙箱无 SDK。
- 提供任何**账号、密钥、证书、隐私政策、应用素材**。
- 真机上的**权限授予、通知/触感的主观体验确认**。

## 沟通约定

- 反馈严格用阶段 3 模板，附设备/通道/截图，我能最快定位。
- 我每次交付会写明：改了哪些文件、怎么验证、你下一步点哪里。
- 模糊需求先问我澄清，再动手，避免返工。

---
*下一步建议：先回「阶段 6 决策清单」5 项，我即可从 2.1 导航重构开工。环境就绪（阶段 0）可与我并行进行。*
