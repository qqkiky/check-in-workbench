# 打卡工作台 · 安卓原生 APP 开发专家团队清单

> 编制视角：当前会话已激活 **MobileApplicationDeveloper（移动应用开发）专家**，按该专家工作流梳理。下方清单覆盖你要求的全部环节（需求分析 → UI/UX → 安卓前端 → 后端接口 → 测试 → 发布），并明确各角色职责、协作方式与决策边界。

## 一、项目现状与目标
- **现状资产**：React 18 + TypeScript + Vite 5 + Tailwind 的 PWA；已安装 Capacitor（`@capacitor/core`、Android 平台、`capacitor.config.json` 的 `appId=com.workbench.checkin`、`webDir=dist`）；`android/` 原生工程已生成，可 `cap sync`；数据层为纯客户端 `localStorage` + `useSyncExternalStore`，**无后端**；当前版本 v26（已部署 PWA）。项目已迁移至 `D:\WorkBuddyProjects\打卡工作台`。
- **目标**：交付可安装/可上架的安卓 APP。技术路线建议 **Capacitor 包裹现有 Web 代码**（复用 ~90%，可上架、原生插件补齐通知/触感/小组件），而非纯 Kotlin 重写（性价比低）。
- **待用户拍板**：是否做云同步（Supabase）、是否一并做 iOS、品牌色与 Tab 顺序。

## 二、专家团队总览

| # | 角色 | 对应能力/专家 | 主责阶段 | 关键交付物 |
|---|------|--------------|---------|-----------|
| 1 | 需求分析专家 | 产品分析（由 MobileApplicationDeveloper 在启动阶段兼任） | 启动 | PRD、MVP 范围、功能优先级矩阵 |
| 2 | UI/UX 设计专家 | 移动端交互/视觉设计 | 设计 | 线框图、设计系统、交互规范、手势/安全区规范 |
| 3 | 安卓前端开发专家 | **MobileApplicationDeveloper（Capacitor/React）** | 开发（主） | 移动端 APP、原生桥接、APK/AAB 构建 |
| 4 | 后端/接口对接专家 | 后端工程（Supabase/自建，按需激活） | 开发（云同步时） | 数据 schema、API 契约、同步/冲突协议 |
| 5 | 测试专家 | QA | 测试 | 测试计划、缺陷报告、回归/性能报告、发布签核 |
| 6 | 发布/运维专家 | DevOps / 上架 | 发布 | 签名 keystore、AAB、商店上架物料、CI/CD |
| 7 | 数据架构专家（可选） | 架构 | 开发 | StorageAdapter 抽象、离线/同步层 |
| 8 | 安全/合规专家（可选） | 安全 | 开发/发布 | 鉴权方案、隐私政策、合规审查 |

## 三、各专家职责与协作

### 1. 需求分析专家
- **职责**：梳理业务场景，定义 MVP 与边界，输出功能优先级，编写验收标准；识别"原生 vs Web"的取舍。
- **本项目要点**：确认 MVP = 打卡+系统通知、一键打卡+补卡选日期、记账（收/支/转账 FAB）、底部 Tab+安全区+触感；确认是否纳入云同步（决定专家 4 是否激活）；确认上架范围（仅 Android / 含 iOS）。
- **协作**：↑ 与用户确认决策；↓ 输出 PRD 给 UI/UX（专家 2）与安卓开发（专家 3）。

### 2. UI/UX 设计专家
- **职责**：移动端信息架构、交互流程、视觉规范；将现有 Web 界面重排为移动优先；定义设计 token（颜色/字号/间距）、安全区、手势、深色模式。
- **本项目要点**：底部 Tab（首页/记账/＋/时间线/更多）+ 中央 FAB；打卡降级为一键即时打卡、时间选择收进次级；图表竖向重排；统一触达反馈。
- **协作**：↑ 接收 PRD；↓ 输出设计稿/标注给安卓开发（专家 3）；与测试（专家 5）对齐验收视觉。

### 3. 安卓前端开发专家（Capacitor/React）— 主开发者（即当前专家）
- **职责**：实现/适配移动端 Web 层；Capacitor 集成与构建；原生插件桥接（通知、触感、可选小组件/生物识别）；处理 WebView 适配（安全区、深色、键盘）；产出可安装构建。
- **现有代码资产可复用**：React 业务代码（打卡/记账/时间线/回顾）、`CheckInTimeModal`（补卡选日期）、`AccountingScreen`（FAB/转账/分类柱状图）、`MoreScreen`（备份恢复）、`android/` 原生壳、`capacitor.config.json`。
- **待建设**：底部 Tab+FAB 导航重构、`Capacitor.LocalNotifications`/`Haptics` 集成、`StorageAdapter→SQLite` 抽象、构建/签名流水线。
- **协作**：↑ 接收设计稿（专家 2）+ 后端契约（专家 4，若云同步）；↓ 交付 QA（专家 5）测试；→ 移交发布（专家 6）。

### 4. 后端/接口对接专家（按需）
- **职责**：云同步后端设计、鉴权、数据增量同步与冲突解决（基于 `updatedAt` + 软删除 `deletedAt`）、API 文档。
- **本项目状态**：当前纯离线、无后端；**仅当用户决定做云同步时激活**。建议 Supabase（Auth + Postgres + Realtime）。
- **协作**：↑ 与安卓开发（专家 3）约定客户端同步逻辑；↓ 提供 API 给测试（专家 5）做接口测试。

### 5. 测试专家
- **职责**：测试策略（功能/兼容/性能/回归）；设备矩阵（真机+模拟器，多 Android 版本/分辨率）；关键场景：通知触发、触感反馈、备份/恢复往返、同步一致性、冷启动<3s、内存<100MB、崩溃率<0.5%。
- **协作**：↑ 接收构建（专家 3）+ API（专家 4）；↓ 缺陷回流给开发；→ 发布签核给运维（专家 6）。

### 6. 发布/运维专家
- **职责**：生成/保管签名 keystore、构建 AAB、Play 商店上架（Listing、截图、隐私政策、分级）、CI/CD、分阶段发布与监控。
- **协作**：↑ 接收签核后的构建（专家 3/5）；需用户/法务提供隐私政策与素材。
- **本项目要点**：当前用 CloudStudio 托管 PWA；原生上架需本机 Android Studio + keystore（沙箱无 SDK，需在用户机器完成）。

### 7–8. 可选专家
- **数据架构**：负责 `StorageAdapter` 抽象与离线优先/同步层，解决 localStorage 5MB 上限隐患。
- **安全/合规**：生物识别解锁、隐私政策合规（尤其涉及健康/财务类数据时）。

## 四、协作方式与流程
- **主线工作流**：需求(1) → 设计(2) → 开发(3 + 4并行) → 测试(5) → 发布(6)。
- **协作机制**：看板跟踪、设计稿标注评审、代码 PR 评审、缺陷回归、发布门禁（测试签核后方可上架）。
- **RACI 示例（以"记账 FAB 功能"为例）**：
  - 负责(R)：安卓前端开发(3)
  - 批准(A)：用户（功能验收）
  - 咨询(C)：UI/UX(2) 提供交互稿、需求(1) 提供范围
  - 知会(I)：测试(5)、发布(6)

## 五、职责边界与需用户拍板事项

| 事项 | 谁提方案 | 谁拍板 |
|------|---------|--------|
| 技术路线（Capacitor vs 纯 Kotlin） | 安卓开发(3) | 用户 |
| 是否做云同步 | 需求(1)/后端(4) | 用户 |
| 上架范围（Android/iOS） | 发布(6) | 用户 |
| 品牌色 / Tab 顺序 / 触感开关 | UI/UX(2) | 用户 |
| 隐私政策与合规责任 | 安全(8)/发布(6) | 用户/法务 |

## 六、现有资产映射
- **已具备（直接复用）**：React 业务代码、Capacitor 工程与 android 壳、`localStorage` 数据层（含 `wb.*` 键全量备份/恢复）、时钟黑字/补卡选日期/账户转账/分类柱状图等已上线特性、v26 部署。
- **待建设**：底部 Tab+FAB 导航、原生通知/触感、SQLite 适配、云同步（可选）、商店上架物料。

## 七、建议的 MVP 切片
- **P0（首轮必做）**：打卡+系统通知、一键打卡+补卡选日期、记账(收/支/转账)+FAB、底部 Tab+安全区+触感。
- **P1**：周/月回顾图、时间线手势编辑、`StorageAdapter→SQLite`。
- **P2（可选/后置）**：主屏 Widget、云同步、年回顾/趋势图、iOS 上架。
- **MVP 明确不做**：云同步（除非拍板）、Widget、年回顾、iOS 上架（可并行不阻塞）。

## 八、关于"专家库"的说明
本清单中的角色按 Android 原生 APP 开发标准团队定义，职责与协作方式已针对本项目落地。**当前会话已选定 MobileApplicationDeveloper 专家作为主开发者**，因此不另行从专家库拉起独立专家实例；若你希望把其中某些角色（如 UI/UX、后端、测试）固化为独立的专家包并在会话中启用，可让我用专家管理工具创建，或在新会话中按需选用对应专家。
