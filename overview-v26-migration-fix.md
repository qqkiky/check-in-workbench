# v26：旧版备份导入兼容修复 + 链接说明

## 一、为什么「链接突然变了」
- 部署工具 `workbuddy_cloudstudio_deploy` 每次返回一个沙箱链接，**不提供「指定/复用某个沙箱」的参数**，返回什么链接就接受什么。
- 此前 v20→v21→v22→v23 几轮，它恰好复用同一沙箱 `74a2a90b…bj8.agentos-app.net`，所以「链接不变」。
- **v24 那次它新建了沙箱** `b0316fa3…app.workbuddy.link`，从此链接变更；v25、v26 又复用这个新沙箱，链接已稳定。
- 这是部署平台分配行为，非人工改址，也无法钉死旧链接。

## 二、你的数据没有丢（v23 打卡记录完好）
- 所有数据存在**浏览器/手机本地的 localStorage**，按「网站来源（域名）」隔离。
- 旧链接 `74a2a90b…bj8.agentos-app.net`（仍为 v23，**HTTP 200 已实测**）持有你**全部的** v23 数据：打卡记录、记账、账户、转账、分类、笔记、时间线。
- 新链接是另一域名的空白 localStorage。部署只换网页代码，从不触碰你浏览器里的数据。
- 结论：**v23 的打卡记录在旧链接里 100% 完好**。

## 三、v26 修复：让「旧链接导出 → 新链接导入」真正可用
### 问题
v23（及以前）的「导出」是残缺格式：只导出 `tasks/records/categories/settings` 四个**裸键名**（不带 `wb.` 前缀，且值为对象而非字符串）。而 v25 新做的全量「导入」只认 `wb.` 前缀 + 字符串值，直接对接会**导入 0 项或数据损坏**——你最关心的打卡记录反而搬不过来。

### 修复（`src/services/storage.ts`）
- `importAllData`：遍历 `data` 时，键名自动补 `wb.` 前缀（裸名 `records` → `wb.records`）；值若非字符串则 `JSON.stringify` 后再存。兼容 v23 导出与 v25 全量导出两种格式。
- `previewBackup`：预览计数同步兼容裸键名，避免显示「0 项」误导。

## 四、v23 打卡记录「怎么办」——两种选择

### 方案 A（零风险，立刻可用）
继续用旧链接 `https://74a2a90b75584bceb0dd46cd1d7430ca.bj8.agentos-app.net`。
- 数据 100% 在（含打卡 + 记账 + 全部），无需任何操作。
- 唯一代价：看不到 v24/v25/v26 的新功能（时钟黑字、分类柱状图、全量备份）。

### 方案 B（迁移到新链接，享新功能）
1. 在**旧链接**点「更多 → 导出数据」→ 下载 `do-backup-*.json`。
2. 在**新链接** `https://b0316fa3e720430387c07bf2e0bf924b.app.workbuddy.link` 点「更多 → 导入恢复 → 选择备份文件」→ v26 兼容层会把打卡记录等正确迁入。
3. 导入后旧链接可弃用。

⚠️ **方案 B 的限制（务必知晓）**：v23 的旧「导出」功能本身**只含 任务/打卡/分类/设置 4 类**——这是 v23 代码的固有限制（当时漏写了记账/笔记/时间线）。所以：
- 若你在旧链接**只有打卡记录** → 方案 B 完美搬回，无损失。
- 若你在旧链接**还有重要记账数据** → v23 导出搬不回记账。最稳是**暂时继续用旧链接**（数据最全）；或告诉我，我评估为你加「记账 CSV 导入」桥接。

## 五、验证
- `npm run build` 成功（`main-Bo89ONbs.js`）；Android 原生壳 `cap sync` 已同步 v26。
- 线上新链接已更新到 v26（SW 缓存 `daka-2026-08-17-v26`）。
- 旧链接实测 HTTP 200、仍为 v23、数据完好。
