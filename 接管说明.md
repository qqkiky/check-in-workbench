# 打卡工作台 - Codex 接管说明

## 项目来源
- 原项目位于 `D:\WorkBuddyProjects\打卡工作台`（WorkBuddy 维护的历史版本）。
- 2026-08-25 由 Codex 接管并建立 git 历史，首次基线提交为 `b3af06d`（对应 WorkBuddy v36 状态）。
- 2026-09-22 完成仓库归并，`D:\WorkBuddyProjects\打卡工作台` 成为唯一日常维护目录；原 C 盘副本已退役。
- 本地完整历史保存在 `archive/local-master-20260922`；公开 GitHub `main` 使用不含内部记忆、临时文件和本地工具的干净源码历史。

## 日常维护方式
- 所有功能修改在 `src/` 下进行，修改后执行 `npm run build` 验证构建。
- 每次修改完成后提交一次 git 记录，方便回退与追溯。
- 日常开发只使用 `D:\WorkBuddyProjects\打卡工作台`，不要再维护其他磁盘副本。

## 线上部署（2026-08-25 起，永久稳定版）
- 线上地址：https://qqkiky.github.io/check-in-workbench/
- GitHub 仓库：https://github.com/qqkiky/check-in-workbench（`main` 存放源码，`gh-pages` 存放部署产物）
- 数据已与本地构建完全一致；离线缓存版本为 `daka-2026-09-01-v41`。
- 更新流程：改代码 → `npm run build` → 用本机 GitHub 命令行把 `dist/` 上传到 `gh-pages` 分支 → Pages 自动更新（令牌已保存在本机 GitHub CLI 配置中）。
- 国内资讯由 `.github/workflows/daily-info-update.yml` 每天北京时间 08:00 自动更新：抓取国内官方源、校验累积数据、构建、提交 `main`、部署 `gh-pages` 并验证线上数据。
- 工作流支持在 GitHub Actions 页面手动触发；当前不自动新增或翻译海外资讯，只保留既有海外历史数据。
- 原 CloudStudio 链接（app.workbuddy.link）已失效，不再使用。

## v38 修复记录（2026-08-25）
- 首页节气倒计时数据只覆盖 2026 年，日期跨到 12 月底后计算陷入死循环，导致页面白屏卡死。
- 已改为按当前年份自动顺延计算，并加安全兜底；线上与本地均已验证恢复正常渲染。

## v39 修复记录（2026-09-01）
- 打卡/任务选择时间时，系统时钟控件在部分手机浏览器（尤其深色模式）下显示白色字体，与背景融为一体。
- 已强制页面使用浅色模式渲染原生时间/日期控件，并加固字体颜色样式；实测字体为深色 `#1C1C1E`。

## v40 更新记录（2026-09-01）
- 记账「统计」里的每日/每月收支柱状图新增三档切换：收支同显 / 仅收入 / 仅支出。
- 月视图显示「每日收支 / 每日收入 / 每日支出」，年视图自动变为「每月收支 / 每月收入 / 每月支出」。

## v41 修复记录（2026-09-01）
- 编辑任务「计划时间」与打卡时间选择里，系统时钟控件在部分手机浏览器仍显示白色字体。
- 已在所有时间/日期输入框上直接加内联深色字体样式（不依赖全局样式表），并升级版本强制刷新手机缓存。

## 注意事项
- 构建（`npm run build`）和 git 提交需要临时授权，因为运行环境对部分目录有保护。
- `.workbuddy/`、本地临时文件、缓存和 `cloudflared.exe` 已加入忽略规则，不得提交到公开仓库。
- 每日资讯自动化已迁移到 GitHub Actions，不再依赖 WorkBuddy、本机 Python 路径或本机 GitHub 令牌。
