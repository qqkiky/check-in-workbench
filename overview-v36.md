# 资讯栏目「没更新」诊断与修复（v36）

## 为什么没更新（两重根因）
1. **PWA 缓存旧数据**：Service Worker 对所有数据 JSON 用「先返回缓存」策略，App 每次打开都拿旧 JSON，后台才悄悄更新——你看到的自然是静止的旧内容。
2. **今日 08:00 自动化没跑**：数据生成时间停在 08-23，今天没成功刷新（自动化配置仍是 ACTIVE，疑似调度/执行侧问题，非代码 bug）。

## 已修复
- `public/sw.js`：数据 JSON 改为「网络优先」，每次打开都拉最新，离线才用缓存。
- 版本号 v35 → v36：缓存名变化，手机端旧缓存被强制清除。
- 数据刷新到今天：国内源新增 2 条（现 行业17 / 药品7 / 监管8），含 FDA 8 月最新召回与指南译文；月归档刷新为 2026-08 / 2026-07。
- 构建已通过（dist/sw.js 已验证注入 network-first、缓存名为 v36）。

## 当前状态
- 代码与 `dist` 已就绪，但 **CloudStudio 部署通道连续报错（401 / fetch failed）**，尚未重新上线。恢复后我会重试部署。
- 你这边：部署成功后，请**硬刷新 / 关闭重开 App 一次**（iOS 上划下拉刷新、安卓 Chrome 菜单刷新）加载 v36；之后资讯将自动拉最新，无需手动刷新。

## 关于「每天更新」的现实
FDA / EMA 等官方源并非每日发公告，某些天"动态条数"不变属正常；只要自动化正常跑，有新增就会自动并入当月模块。若明天 08:00 仍不见自动更新，请反馈，我再深入排查自动化执行日志。
