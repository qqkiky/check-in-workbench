# 记账模块 · 账户间转账（v21）

## 做了什么

给记账模块加了账户之间相互转账的能力：从 A 账户划钱到 B 账户，支持手续费、
历史时间补录、备注，可编辑可删除，余额自动双向同步。

## 核心设计决策

**转账用独立数据实体，不混进账目（Transaction）。**

如果把转账当成"一笔支出 + 一笔收入"记进账目，会出现这些问题：

| 受影响的地方 | 混入账目的后果 |
|---|---|
| 月度收入/支出 | 自己给自己转 1000 元，虚增 1000 收入 + 1000 支出 |
| 预算进度条 | 转账被当成消费，预算瞬间"超支" |
| 分类占比环形图 | 出现一个不存在的"转账"分类挤占真实消费占比 |
| 结余计算 | 收支两端抵消看似无害，但分母被撑大，占比全部失真 |

所以新增了独立的 `Transfer` 实体，与账目彻底解耦。好处是现有全部统计逻辑
一行未改、零回归风险，而转账数据依然能在流水里正常展示。

## 余额规则

```
转出账户 = 原余额 − 转账金额 − 手续费
转入账户 = 原余额 + 转账金额
```

转账在两个账户之间对冲，**不影响净资产**；只有手续费是真实流失，会让净资产减少。
这也是判断实现是否正确的最好自检点。

## 功能清单

### 转账弹窗
- 转出 / 转入账户横向选择，各自实时显示当前余额
- 两者互斥：已选为转出的账户在转入列表中自动置灰
- 中间「对调」按钮，一键交换转出与转入
- 数字键盘输入金额（与「记一笔」一致的手感）
- 可选手续费，额外从转出账户扣除
- **转账后余额预览**：确认前就能看到两个账户各自的余额变化
- 余额不足时给黄色警告（信用卡账户除外，欠款为负是正常的）
- 日期 / 时间可改，支持补录历史转账
- 编辑与删除已有转账

### 记账页接入
- 头部新增「转账」按钮
- 流水列表中转账与账目**按时间混排**，紫色双向箭头图标区分，显示「A → B」流向
- 类型筛选新增「转账」一档；选中时展示笔数、划转总额与手续费小计
- 日期分组小计里，转账单独用「转 xxx」呈现，不掺进收/支数字
- 账户页余额已计入转账，负余额标红；每个账户右侧有快捷转账按钮（自动预填为转出方）
- 净资产卡片下方显示本月转账笔数与划转总额
- CSV 导出包含转账行：类型「转账」、账户列「现金 → 银行卡」、手续费并入备注

## 交互细节处理

- 编辑转账时，余额预览会先剔除这笔转账自身再计算，避免重复扣减
- 切到「转账」筛选时自动清空分类筛选（转账本就没有收支分类，否则列表恒为空）
- 账户筛选对转账匹配转出/转入任意一方
- 账户不足 2 个时，转账入口置灰并提示先去添加账户

## 验证

- TypeScript 类型检查通过
- 生产构建成功（878 模块）
- 版本号 `2026-08-12-v21`，Service Worker 缓存名同步为 `daka-2026-08-12-v21`
- 线上资源验证：index.html 引用的 JS/CSS 均返回 200

**访问链接（不变）**：https://74a2a90b75584bceb0dd46cd1d7430ca.bj8.agentos-app.net
