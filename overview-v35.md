# 资讯栏目紧急修复（v35 / 2026-08-23-v35）

## 问题现象
用户反馈：点进「每月资讯」就**卡成灰色、退不出**（底部 Tab 也切不动）。

## 根因
`src/screens/InfoScreen.tsx` 的 `MonthlyModule` 在渲染月报卡片副标题时，调用了
`len(m.sections.industry)` 等——但整个文件**只定义了 `sumLen`，从没定义过 `len`**。

这是运行时 `ReferenceError`：
- 切到「每月资讯」→ 渲染到该卡片 → 调用未定义函数 → React 子树崩溃。
- PWA（iOS Safari/WKWebView）中组件崩溃表现为整页灰屏/冻结，且因错误冒泡到根，底部 Tab 的事件也可能被冻结，于是"退不出"。
- 其它三个模块（行业动态/药品信息/监管动态）的渲染路径不触发 `len`，所以正常。
- vite 用 esbuild 转译，**不会**在构建期报"未定义变量"，因此上线后才暴露。

## 修复
1. **修崩溃源**：第 300 行改为 `(m.sections?.industry || []).length` 等，并加可选链 `?.` 防止数据字段缺失时再次崩溃。
2. **加错误边界（兜底）**：新增 `ModuleErrorBoundary`（class 组件，需 `import React`）包裹四个模块。
   - 今后任一模块再抛错，只降级显示该模块 + "重试"按钮，**不再冻结整页、Tab 始终可切换**。
3. 版本号 `2026-08-23-v34` → `2026-08-23-v35`。

## 验证
- `vite build` 成功（884 模块）；grep 确认 dist bundle 中 `行业 {len` 已消失。
- 已部署至稳定链接：`https://b0316fa3e720430387c07bf2e0bf924b.app.workbuddy.link`

## 用户侧动作
App 内**硬刷新/关闭重开**（iOS 上划下拉刷新、安卓 Chrome 菜单刷新）即可加载 v35，
此后点「每月资讯」正常：默认折叠两个月归档，点卡片展开看概览/核心要点/三类明细，可自由切回其它 Tab。
